# 飞牛影视（trim-media）刮削接口协议

> 基于对 `tmdb_provider.py` 代码与 `logs/requests_tmdb.log` 真实请求的逆向整理。
> 整理日期：2026-09-10。链路：飞牛 UI → trim-media 后端(:8005) → 自建 provider(:38080) → TMDB / 字幕服务。
> 飞牛 HTTP 客户端为 Go + `go-resty/2.13.1`，请求带 `Authx: nonce=..&timestamp=..&sign=..` 签名头（原版 mediasvc 校验此头；自建 provider 不校验）。

---

## 一、通用约定

### 响应信封

所有 JSON 接口统一三段式：

```json
{"code": 0, "msg": "", "data": { ... }}
```

| code | 含义 |
|---|---|
| 0 | 成功 |
| 404 | 接口不存在 / 未匹配（msg: "not found"） |
| 5000 | provider 内部异常（msg: 异常文本） |

注意：HTTP 状态码几乎总是 200（连 404 也是 HTTP 200 + body code=404），飞牛靠 body.code 判断。

### 图片路径约定

响应里的海报/剧照一律是**裸 TMDB 路径**（如 `/gjktze6OF0wESkbHU4h3dxcWNoe.jpg`），
飞牛/后端自行拼 `<源地址>/t/p/{size}<裸路径>` 回源到 provider 的图片代理。

### trim_id 格式

provider 对外一律 `tm<TMDB数字id>`（如 `tm94664`）。
注意：**飞牛数据库内部存储时会改前缀为 `tt`**（`tt94664`），直接查库时别用 tm 前缀。

---

## 二、接口明细

### 1. GET /healthz

健康探测。trim-media 后端与字幕源**各每 30 秒轮询一次**（日志里可见两组规律心跳）。

```json
{"code": 0, "msg": "", "data": {"status": "ok"}}
```

### 2. GET /match

原版源即返回 404 的占位接口。provider 原样返回 `404 page not found`（text/plain）。

### 3. POST /search/item —— 单文件识别（核心）

触发场景：扫描新文件、重新刮削、手动"识别"。**每个文件一次**。

请求：

```json
{
  "fileName": "/vol2/1000/anime/downloads/Bangumi/无职转生～到了异世界就拿出真本事/Season 1/无职转生～到了异世界就拿出真本事 S01E02.mkv",
  "fileHash": "6fa20bd316008f07",
  "language": "zh-CN",
  "nfo": {"season": -1, "episode": -1, "type": "", "source": "", "sourceId": ""},
  "includeAdult": false,
  "thirdPartyHash": ""
}
```

- `nfo.season/episode = -1` 表示未指定，按 fileName 解析；
- **手动指定季/集时 nfo 填实际值**（如 `{"season":0,"episode":2,"type":"Episode","source":"trim_id","sourceId":"tm94664"}`），
  **`0` 是合法值（第 0 季=特别篇），不能当缺失处理**；
- `fileHash` 由飞牛生成（路径/内容相关），同一文件重刮时值不变。

响应：

```json
{"code": 0, "msg": "", "data": {
  "cleanData": {"trimId": "tm94664", "tmdbId": 94664, "imdbId": "", "doubanId": 0, "pinYin": {}},
  "episode": {
    "trim_id": "tm94664", "tmdb_id": 94664, "imdb_id": "", "pinYin": {},
    "air_date": "2021-01-18", "episode_number": 2, "name": "老师",
    "overview": "3岁便表现出魔术才能的鲁迪乌斯……",
    "runtime": 24, "season_number": 1,
    "still_path": "/gjktze6OF0wESkbHU4h3dxcWNoe.jpg",
    "vote_average": 5.0, "vote_count": 1,
    "episode_imdb_id": "", "data_version": "5e6ec94b…"
  }
}}
```

电影/无集号文件时 `episode` 为 `null`。

### 4. POST /search/multi —— 关键字搜索候选

触发场景：编辑元数据 → 手动搜索标题。

```json
{"keyword": "无职转生", "keywordType": "title", "language": "zh-CN", "includeAdult": false}
```

```json
{"code": 0, "msg": "", "data": {"list": [{
  "source": "trim_id", "sourceId": "tm94664", "type": "tv",
  "name": "无职转生～到了异世界就拿出真本事～",
  "posterPath": "/u7LWdKmEdEr6Ui3GZMsFGlKZQBd.jpg",
  "genres": [], "productionCountries": [],
  "firstAirDate": "2021-01-11", "lastAirDate": "",
  "numberOfSeasons": 3
}]}}
```

注意 `numberOfSeasons` 必须给真实结构季数（TMDB 搜索接口没有该字段，需逐个补查；传 0 会导致飞牛选中候选后无后续动作）。

### 5. POST /detail/tv —— 剧集详情（决定季结构）

触发场景：识别成功后飞牛拉取整剧结构并建 TV/Season/Episode 条目。

```json
{"sourceId": "tm94664", "language": "zh-CN", "source": "trim_id", "isRescrap": false}
```

响应 `data.tv` 顶层字段（实测）：

```
trim_id, id, imdb_id, pinYin, first_air_date, genres[{id,name}], last_air_date,
name, number_of_episodes, number_of_seasons, origin_country, original_name,
overview, poster_path, production_countries, status, vote_average, vote_count,
alternative_titles, content_ratings, images, keywords, adult, data_version, seasons[]
```

`seasons[]` 元素：

```json
{"id": "1", "name": "第 1 季", "overview": "…", "poster_path": "/u7LWdK….jpg",
 "season_number": 1, "episode_count": 23, "air_date": "2021-01-11"}
```

**两个致命坑（都踩过）**：

1. `seasons[].id` 飞牛 Go 结构体是 **string**，返回数字会导致整个响应
   unmarshal 失败 → 刮削结果整单丢弃（`json: cannot unmarshal number into Go
   struct field TvSeasons.data.tv.seasons.id of type string`），文件全部停在未识别状态。
2. **第 0 季（特别篇）必须包含在 seasons[] 里**，否则 S00Exx 的集会被飞牛塞进第 1 季。
   `number_of_seasons` 只计 S1+，S0 不计入。

### 6. POST /detail/tv/season —— 季详情

```json
{"sourceId": "tm94664", "language": "zh-CN", "source": "trim_id", "season": 0, "isRescrap": false}
```

`season=0` 合法（特别篇）。响应 `data.season`：

```
trim_id, tmdb_id, imdb_id, pinYin, air_date, name, overview, poster_path,
season_number, episode_count, episodes[集数组，字段同 search/item 的 episode +
credits, images], data_version
```

**坑**：飞牛对第 0 季的请求体里 **season 字段整个缺失**（Go int + omitempty，
实测只有 `{"sourceId","language","source","isRescrap"}`），文档上写了 season=0
但线上不会送达。provider 不能把"缺 season"默认成 S1（无职转生 Season 0 实测
被刮成第 1 季），现按本剧最近一次 `/search/item` 命中的季兜底（短 TTL）；
`/detail/tv/season/episode`、`/meta/diff`（seasonNumber）同理。

### 7. POST /detail/tv/season/episode —— 单集详情

```json
{"sourceId": "tm94664", "language": "zh-CN", "source": "trim_id", "season": 1, "episode": 8, "isRescrap": false}
```

响应结构同 search/item 的 `episode`。飞牛扫描时会**按季逐集全量拉取**（一个 60 集的剧 ≈ 60 次请求，约 1-2 秒一次）。

### 8. POST /meta/diff —— 差量比对（决定"刷新"是否生效）

触发场景：刷新元数据 / 重新刮削已识别的条目。飞牛带本地存的 `dataVersion`，provider 比对后决定是否回传新数据。

```json
{"category": "tv",     "language": "zh-CN", "trimId": "tm94664", "dataVersion": "…"}
{"category": "season",  "language": "zh-CN", "trimId": "tm94664", "seasonNumber": 0, "dataVersion": "…"}
{"category": "episode", "language": "zh-CN", "trimId": "tm94664", "seasonNumber": 0, "episodeNumber": 2, "dataVersion": "…"}
```

响应：

```json
{"code": 0, "msg": "", "data": {"hasDiff": false}}
{"code": 0, "msg": "", "data": {"hasDiff": true, "tv": {…同 detail/tv 的 tv，去掉 data_version}}}
{"code": 0, "msg": "", "data": {"hasDiff": true, "season": {…}}}
{"code": 0, "msg": "", "data": {"hasDiff": true, "episode": {…}}}
```

**坑**：`seasonNumber=0`（特别篇）曾被 `or 1` 写法换成 S1 比对 → 数据"恰好一致" →
`hasDiff=false` → 飞牛永远不更新错误数据。任何 `season or 1`、`season <= 0` 写法都是 bug。

### 9. POST /meta/images —— 编辑元数据"搜索图片"

触发场景：编辑元数据 → 点某个海报/背景图字段的搜索按钮（前端实际调后端 `POST /mdb/getPoster`，后端转发）。

```json
{"category": "tv", "language": "zh-CN", "trimId": "tm65942"}
```

响应：

```json
{"code": 0, "msg": "", "data": {
  "poster": "/a1pMK4456dF2j5B9xvkjMEGiOOw.jpg",
  "posters": ["/pyjIFQAJ….jpg", "…"]
}}
```

- `poster`：当前默认海报；`posters`：候选列表，**字符串数组**（前端去重后取前 3 个做预览，网格展示全部）；
- UI 拉取候选图时按 `/t/p/original<裸路径>` 回源；
- 未实现时返回 code=404，前端显示"网络异常，请检查后重试"。

### 10. POST /genres

```json
{"code": 0, "msg": "", "data": {"genres": [{"id": 10759, "name": "动作冒险"}, {"id": 16, "name": "动画"}, …]}}
```

### 11. 图片代理 GET /t/p/{size}/{path}

- size：`original` / `w780` / `w500` / `w300` / `w92` …（飞牛网格用 original，列表用小尺寸）
- 兼容双重前缀：`/t/p/original/t/p/w300/x.jpg` → 取最后一个 `/t/p/` 后的子路径；
- 回退链：飞牛本地缓存文件 → TMDB（keep-alive 连接复用，按图片类型自动降尺寸），磁盘缓存于 `cache/img/`。

### 12. 字幕 /v1/*

原样转发到 subtitle-service.fnnas.com（飞牛字幕源指向 provider 时生效）。

### 13. 未实现（有意不提供）

| 接口 | 说明 |
|---|---|
| `POST /search/by`、`POST /search/byThirdPartyHash` | 电影识别接口，未实现（返回 404）；现有电影库数据为历史刮削遗留 |
| `GET /lan/iso6391` `iso6392` `iso3166` | 语言/地区码表，未实现 |
| `POST /detail/person` | 人物详情，占位 404 |

---

## 三、飞牛侧内部存储（排查用观测值）

数据库：`/usr/local/apps/@appdata/trim.media/database/trimmedia.db`（主库）+ `trimmedia_ext.db`（存档）。

- `item.type` 取值：`TV` / `Season` / `Episode` / `Movie` / `Video`（未识别的裸文件）/ `Directory`（文件夹节点，**库页面把无主文件夹当作品展示**的来源）；
- `item.trim_id` 前缀为 **tt**；`tmdb_id` 为数字；
- `item.posters / still_path` 存**飞牛本地缓存路径**（`/xx/yy/<TMDB文件名>`，两级目录规则未逆向；provider 数据更新后飞牛会自行下载到新路径）；
- `item.fetch_status`：3 = 未识别/失败；
- `trimmedia_ext.db.item_archive`：**刮削结果存档缓存**（trim_id+季+集+语言 → data/data_version）。
  它会"重放"旧识别结果——provider 修好后重新刮削若仍显示旧数据，需清空对应 trim_id 的存档（或删条目重扫）；
- 前端对刮削相关操作走后端自有 API（如 `POST /mdb/getPoster` → provider `/meta/images`、
  `POST /scrap/rescrap` → search/item + detail/tv + meta/diff 链），部分操作有**按 fileHash 的结果缓存**，
  重刮不产生新 provider 请求时就是命中了缓存——改文件名/删条目重扫可强制重新识别。

---

## 四、历史踩坑索引（按发现顺序）

1. `seasons[].id` 返回数字 → detail/tv 整单解析失败，全部文件未识别，库页面只剩"Season N"文件夹卡片；
2. `build_tv_out` 季列表 `range(1, n+1)` 漏掉 S0 → S00Exx 被塞进第 1 季；
3. 5 处 `or 1` / `<= 0` 把季 0 当缺失值 → 手动指定第 0 季无效、特别篇显示第 1 季数据、meta/diff 永远 hasDiff=false（刷新无效）；
4. `_handle` 调试代码在 `path` 赋值前引用它 → 所有 POST 接口崩溃（healthz 正常，前端报"无法连接服务器"）；
5. `_save_caches` 对已是 str 的 img_feat 调 `.hex()` → meta/images 间歇 500；
6. 飞牛按 fileHash 缓存识别结果 → provider 修复后重刮"没变化"，需删条目/清 item_archive 强制重识别。
